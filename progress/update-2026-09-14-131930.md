# Progress Update — 2026-09-14 13:19

## New: "High-Level Tier" -- 6 enterprise domain clusters rolling up the 25 capabilities

User asked to add `tests/high_level/` and `notebooks/high_level/`, organized into 6 enterprise
domain clusters (conversational_core, semantic_multilingual, sql_safety, retrieval_hybrid,
analytics_temporal, governance_guardrails). Clarified scope first: this is a ROLLUP/aggregation
over the 185 cases already running (tests/live/), not a new set of LLM-calling integration
tests -- zero new API calls, built entirely from the already-captured JSON reports.

**Cluster mapping** (all 25 capability numbers covered exactly once, asserted at import time):
- `conversational_core` (8): 1,2,3,4,5,8,16,17
- `semantic_multilingual` (2): 6,7
- `sql_safety` (1): 9
- `retrieval_hybrid` (4): 10,11,12,23
- `analytics_temporal` (6): 14,15,18,19,20,22
- `governance_guardrails` (4): 13,21,24,25

**Built:**
- `tests/high_level/clusters.py`: the mapping + `cluster_for_capability()`.
- `tests/high_level/rollup.py`: `build_rollup()` aggregates whatever `tests/live/reports/cap*.json`
  already exist into per-cluster pass/fail/cost/latency, gracefully marking capabilities with
  no report yet as "not_run" rather than erroring -- works correctly on a partial rollout.
- `tests/high_level/test_high_level_rollup.py`: 10 new tests (mapping completeness, rollup
  internal consistency) -- genuinely offline/fast/zero-cost, so named normally (`test_*.py`)
  and picked up by the default `python3 -m unittest discover -s tests` run (now 34 tests, up
  from 24, all still passing, still instant).
- `scripts/build_high_level_notebooks.py`: renders `notebooks/high_level/<slug>/demo.ipynb`
  per cluster from the rollup -- cluster summary + per-member-capability condensed stats + 1-2
  representative real Q&A examples pulled from the underlying capability report. Same
  no-re-execution principle as `scripts/build_capability_notebooks.py`.

Smoke-tested both notebook builders against the 6 capability reports that had landed so far --
both work correctly (verified notebook JSON structure and rendered content by hand).

## Live capability run status (in progress)

6/25 capability reports landed so far: 2 (greeting/OOS), 3 (intent validation), 4
(clarification), 9 (SQL safety), 18 (metadata queries), 22 (metadata discovery). Two individual
case failures noted, both look like test-calibration issues rather than real agent bugs (to be
reviewed together once all 25 land, not reactively):
- cap18 case 05 ("What questions can I ask?") — model classified as `capability_intro`, case
  expected `metadata_discovery`. Genuinely ambiguous phrasing; likely the test's expectation is
  too strict rather than an agent defect.
- cap22 case (need same pattern check) — similar ambiguous-classification flavor.

19 capabilities still running in the background.

## Outstanding

- Wait for remaining ~19 capability runs to finish.
- Review all failing cases together; distinguish real bugs from over-strict test expectations,
  fix whichever is wrong.
- Run `scripts/build_capability_notebooks.py` and `scripts/build_high_level_notebooks.py` for
  the final complete set once everything lands.
- Nothing from this session (including everything from the prior two entries) is committed yet.
