# The core runtime (src/) has ZERO required third-party dependencies -- it's
# pure Python standard library (sqlite3, json, re, math, statistics, io,
# signal). This was a deliberate design choice for portability and to keep
# the offline/mock smoke tests (tests/test_pipeline.py) runnable anywhere.
# See docs/DESIGN_DECISIONS.md.
#
# Install only the extras you actually need:

# --- LLM provider: install ONE of these to match LLM_PROVIDER ---
anthropic>=0.40.0
openai>=1.50.0

# --- Web search sub-agent (optional) ---
# requests is needed only if TAVILY_API_KEY is set (production search provider)
requests>=2.31.0
# duckduckgo-search is an optional free/no-key fallback for the demo
duckduckgo-search>=6.0

# --- Notebook ---
jupyter>=1.0.0
notebook>=7.0.0

# --- Developer-mode UI (optional) ---
# only needed to run `streamlit run ui/app.py`
streamlit>=1.38.0

# --- Semantic document retrieval: DELIBERATELY NOT listed here ---
# fastembed adds a real, measured ~288MB of process memory once loaded (see
# docs/DESIGN_DECISIONS.md #6) -- too much to put in this file unconditionally,
# since anything platform-tooling (incl. Streamlit Community Cloud) installs
# straight from requirements.txt with no concept of "optional" the way
# pyproject.toml's [project.optional-dependencies] has. Without it, retrieval
# silently falls back to BM25 + metadata only (the original behavior) -- which
# is the safe default for a 1GB-RAM deploy target. To opt in deliberately:
# see requirements-embeddings.txt.
